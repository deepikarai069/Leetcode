class Solution {
    public boolean uniformArray(int[] nums1) {
        // Any single element can always be chosen as it is.
        // If there are both even and odd numbers, we can make
        // every element have the same parity using subtraction.

        return true;
    }
}